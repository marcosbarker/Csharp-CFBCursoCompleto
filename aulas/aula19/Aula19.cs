using System;

class Aula19 {
  static void Main() {
    //loop for (para) / estruturas de iteracao
    //for (inicializador; definicao da repeticao; incrementoOuDecremento)
    //for (int num=0; num < 1; num++)
    //for (e1; e2; e3) 
    //e1: executa apenas na primeira vez
    //e2: testa, executa os comandos
    //e3: incrementa, volta para e2

    for (int num=0; num < 10; num++) {
      Console.WriteLine("Teste de repetição, valor: " + num);
    }
  }
}